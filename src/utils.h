#ifndef UTILS_H
#define UTILS_H

char * chomp          (char *str);
char * trim           (char *str);
int    cmpstringp     (const void *p1, const void *p2);
int    casecmpstringp (const void *p1, const void *p2);

#endif /* utils.h */
